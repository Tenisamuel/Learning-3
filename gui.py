import tkinter as tk

#widgets: designed to make buttons, windows, interfaces, menus, pointers.
#windows: a container to hold the widgets.

window = tk.Tk() #creates a window object
window.title("Xnec") #sets the title of the window

window.mainloop()
